# 紅海釣具 — 視覺與互動設計綱要

> 本文件是純文字的設計規範（Design Spec），用於指導整站重建。
> **不含任何圖片**；所有 mockup、產品圖、形象圖留待未來生成。
> 規範以「現代戶外裝備品牌」為基調，貼合磯釣 / 船釣 / 灘釣 / 路亞 / 釣蝦 等多元釣具型態。

---

## 1. 設計理念（Design Principles）

| 原則 | 內容 |
| --- | --- |
| **海洋感而不土味** | 取海色不取俗藍；磯岩、深海、夜釣的層次感取代螢光與爆炸特價貼 |
| **裝備感** | 像戶外品牌（Patagonia、YETI、Daiwa Global、Shimano Premium）那樣，把器材視為精選器物，不是百貨花車 |
| **資訊密度高但有節奏** | 釣具 SKU 多、規格細，需要排得密但不亂；用節奏感取代留白浪費 |
| **內容驅動** | 商品圖、規格、編號、價格才是主角；裝飾、特效退到輔助 |
| **跨裝置一致** | 手機是主要購買裝置（原站已有 mobile_*.css），桌機只是更舒展，不是另一種風格 |
| **可長期維護** | 商品資料來自定時 DB → JSON，UI 不可以與資料 schema 強耦合 |

---

## 2. 色彩系統（Color Tokens）

### 2.1 主色：深海（Deep Sea）
模擬磯岸夜釣與深水船釣的氛圍，作為品牌主色與整體背景基調。

| Token | HEX | 用途 |
| --- | --- | --- |
| `--ink-900` | `#0B1B2B` | 主背景（深色模式預設） |
| `--ink-800` | `#11293E` | 卡片/容器底色 |
| `--ink-700` | `#1B3950` | hover、選中態的容器底色 |
| `--ink-500` | `#2F5874` | 分隔線（深色模式） |
| `--ink-300` | `#6F8AA3` | 次級文字（深色模式） |
| `--ink-100` | `#D7E2EC` | 主要文字（深色模式） |
| `--ink-050` | `#F4F7FA` | 主背景（淺色模式） |

### 2.2 強調色：磯岩與漁火
取自磯岸礁石、漁港燈火、夕陽下的海面反光，作為價格、CTA、特價標的點綴。

| Token | HEX | 用途 |
| --- | --- | --- |
| `--brand-tide` | `#0E7C86` | 品牌色（鏈接、按鈕填色、focus ring）。中度藍綠 |
| `--brand-tide-strong` | `#0A5F66` | 按鈕 hover/按下 |
| `--brand-tide-soft` | `#E7F4F5` | 高亮背景、tag |
| `--accent-ember` | `#E15D2F` | 特價標、限量、優惠標籤（克制使用） |
| `--accent-amber` | `#F4B400` | 評等、會員等級、推薦徽章 |
| `--accent-kelp` | `#3F7D4E` | 庫存正常、訂單已完成等正向狀態 |

> **特價標規範**：用 `--accent-ember` 純色實心徽章 + 白字，避免使用紅色霓虹漸層。

### 2.3 中性灰（淺色模式 / 表單）
| Token | HEX | 用途 |
| --- | --- | --- |
| `--gray-900` | `#1F2937` | 文字主色（淺色模式） |
| `--gray-700` | `#4B5563` | 次級文字 |
| `--gray-500` | `#9CA3AF` | placeholder |
| `--gray-300` | `#D1D5DB` | 邊框、分隔線 |
| `--gray-100` | `#F3F4F6` | 卡片背景 |

### 2.4 語義色
| Token | HEX | 用途 |
| --- | --- | --- |
| `--status-success` | `#10B981` | 成功 |
| `--status-warning` | `#F59E0B` | 警告 |
| `--status-error` | `#EF4444` | 錯誤 |
| `--status-info` | `#3B82F6` | 資訊 |

### 2.5 雙模式（Light / Dark）
- **預設 = Light**（依 mockup 與電商實務：白底攝影感、商品為主角）
- Hero、Footer、Brand wall 等局部用深色（`--ink-900`）作為氣勢區塊
- 使用者可切 Dark：用 `prefers-color-scheme` + 手動 toggle
- 切換策略：用 CSS 變數對應到 `[data-theme="dark"]` 與 `[data-theme="light"]`

---

## 3. 字體系統（Typography）

### 3.1 字型選擇
| 用途 | 字體（候選） | 備援 |
| --- | --- | --- |
| **中文主體** | Noto Sans TC（思源黑體 / Source Han Sans TC） | system-ui, "PingFang TC", "Microsoft JhengHei" |
| **英文/數字** | Inter | -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto |
| **數字（價格）** | Inter Tabular Nums (`font-variant-numeric: tabular-nums`) | — |
| **品牌/標題（可選）** | Bebas Neue 或 Anton（粗壯展示字） | Inter Black |

避免使用：標楷體、新細明體、Comic Sans、Times New Roman。

### 3.2 字級階層（rem，以 16px 為基準）

| Token | rem | px | 用途 |
| --- | --- | --- | --- |
| `--text-xs` | 0.75 | 12 | 標籤、breadcrumb |
| `--text-sm` | 0.875 | 14 | 輔助說明、表單 hint |
| `--text-base` | 1.0 | 16 | 內文 |
| `--text-lg` | 1.125 | 18 | 商品名（列表卡片） |
| `--text-xl` | 1.375 | 22 | 區塊標題 |
| `--text-2xl` | 1.75 | 28 | 商品名（詳情頁） |
| `--text-3xl` | 2.25 | 36 | 頁面主標 |
| `--text-display` | 3.0 | 48 | 首頁 hero、品牌語句 |

### 3.3 字重
- 中文：300（情境）、400（內文）、500（強調）、700（標題）
- 英文：400 / 500 / 600 / 700

### 3.4 行高
- 標題：1.2
- 中文長文：1.7
- 英文長文：1.5
- 商品名（一行截斷）：1.3

---

## 4. 間距系統（Spacing & Layout）

### 4.1 間距 token（8px 基底）
| Token | px |
| --- | --- |
| `--space-1` | 4 |
| `--space-2` | 8 |
| `--space-3` | 12 |
| `--space-4` | 16 |
| `--space-5` | 24 |
| `--space-6` | 32 |
| `--space-7` | 48 |
| `--space-8` | 64 |
| `--space-9` | 96 |

### 4.2 容器寬度
- 全局 max-width：**1280px**
- 商品詳情主欄：**720px**（文字易讀寬度）
- 響應斷點：
  - sm: 480
  - md: 768
  - lg: 1024
  - xl: 1280

### 4.3 商品卡片格線
| 斷點 | 欄數 | gap |
| --- | --- | --- |
| < 480px | 2 | 8px |
| 480 – 767 | 2 | 12px |
| 768 – 1023 | 3 | 16px |
| 1024 – 1279 | 4 | 20px |
| ≥ 1280 | 5 | 24px |

> 釣具商品在桌機需要呈現足夠選擇感，因此預設 4–5 欄而非常見的 3 欄。

### 4.4 圓角 / 陰影
| Token | 值 |
| --- | --- |
| `--radius-sm` | 4px |
| `--radius-md` | 8px |
| `--radius-lg` | 12px |
| `--radius-pill` | 999px |
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.08)` |
| `--shadow-md` | `0 4px 12px rgba(0,0,0,0.12)` |
| `--shadow-lg` | `0 12px 32px rgba(0,0,0,0.18)` |

商品卡片**預設無陰影**（極簡），hover 才上 `--shadow-md`。

---

## 5. 元件規範（Components）

### 5.1 Header（頂部導航）
- 高度：64px（桌機）/ 56px（手機）
- 結構（左→右）：Logo + 搜尋框 + Nav（11 大類） + 訂單查詢 + 會員 + 購物車徽章
- 滾動時：scroll > 80px 加入 `backdrop-filter: blur(12px)` 與細邊
- 11 大類在桌機橫排，手機收進漢堡選單
- 子分類（每大類多達 18 個子分類）用 mega menu 三欄展開，hover 200ms 漸出

### 5.2 Logo / 品牌語
- 文字商標：「紅海釣具」+ 英文輔助 `FishTool.tw`（取原站既有品牌字標，斜體小字、低調 .tw 域名感）
- mark：魚鉤輪廓 + 海浪線條（modern 線稿，呼應原站紅色手寫 + 小魚但更乾淨）
- 品牌語（hero 用）：「探索無限可能 釣出你的精彩」

### 5.3 SearchBar
- 圓角 24px、placeholder：「搜尋釣竿、捲線器、品牌、編號」
- 桌機：頂部 nav 中央寬度約 320–480px
- 手機：點擊放大鏡展開全寬 modal 搜尋
- 支援按 `Esc` 關閉、`/` 開啟（鍵盤快捷）

### 5.4 商品卡片（ProductCard）
```
┌────────────────────┐
│                    │
│   [product image]  │ ← 1:1，object-fit: contain, 背景 --ink-800
│                    │
├────────────────────┤
│ 品牌 · 編號         │ ← --text-xs --ink-300
│ 商品名稱（最多兩行）  │ ← --text-lg --ink-100
│ $1,580 (原 $1,750) │ ← 特價大字 + 原價刪除線
│ [特價] [新品]       │ ← 徽章
└────────────────────┘
```
- 整卡片可點，hover 圖片放大 1.03 倍 + 浮上陰影
- 售價（`price`）與特價（`sale_price`）若相等只顯示一個
- 「滿千免運」之類的活動標 → 用小型 tag 顯示在卡片右上

### 5.5 商品詳情頁（ProductDetailLayout）
桌機版：左 60% 圖庫 / 右 40% 資訊
```
┌──────────────────────────────────┬────────────────────┐
│                                  │ 麵包屑              │
│         [主圖 1:1]                │ 品牌 · 編號 DM-xxx  │
│                                  │ 商品名稱（h1）       │
│ [縮圖 1][縮圖 2][縮圖 3] …        │ ★ 評等（如有）       │
│                                  │ ─────────────       │
│         說明（h2 + 內文）          │ $ 售價 / 特價       │
│                                  │ 規格選擇（如尺寸）    │
│         規格表（spec table）       │ 數量 + 加入購物車    │
│                                  │ 收藏 / 分享          │
│         相關商品                   │ 庫存 / 配送資訊      │
│                                  │ FAQ 摺疊            │
└──────────────────────────────────┴────────────────────┘
```
手機版：圖庫橫滑、資訊在下、加入購物車 sticky 至底部。

### 5.6 麵包屑（Breadcrumb）
`首頁 / 釣竿 / 釣竿-磯釣竿 / 《SHIMANO》(特價) 17 HOLIDAY ISO`
- 字級 `--text-xs`、`--ink-300`、最後一節 `--ink-100`
- 分隔符用 `/` 而非 `＞`（原站用全形 ＞，現代版改半形）

### 5.7 分類側邊導航（CategorySidebar）
- 11 大類為 accordion，當前大類預設展開
- 子分類用條列、選中態加左側 4px 寬 `--brand-tide` 色條
- 桌機分類頁：左 sidebar 200px + 右商品格線
- 手機分類頁：頂部水平捲動 chip bar

### 5.8 篩選與排序（Filter & Sort）
- 排序：價格 ▲ / 價格 ▼ / 上架新到舊 / 上架舊到新
- 篩選（建議新增）：品牌、價格區間、是否有特價、是否有庫存
- 篩選 chip 顯示在格線上方，可一鍵清除

### 5.9 按鈕（Button）
| 變體 | 用途 | 樣式 |
| --- | --- | --- |
| Primary | 加入購物車、結帳 | 填色 `--brand-tide`、白字 |
| Secondary | 取消、返回 | 透明 + 邊框 `--ink-500` |
| Ghost | 列表內的次要操作 | 純文字、hover 加底 |
| Danger | 刪除、移除 | 填色 `--status-error` |
| Pill | 篩選 chip | 圓角 999、邊框 + hover 換底 |

- 高度：lg 48 / md 40 / sm 32
- 內距：橫向 16-24
- focus ring：2px outline `--brand-tide` + 2px offset

### 5.10 表單（Form）
- input 高度 40 / 圓角 6 / 邊框 1px `--gray-300`
- focus：邊框 `--brand-tide`、外加 2px outline
- 錯誤訊息：紅字 12px、icon `!` 在前
- 必填用 `*` 紅色標示，hint 顯示在 label 旁灰字

### 5.11 徽章 / 標籤（Badge）
- 規格：高 20、圓角 4、字級 11、字重 600
- 變體：特價、新品、現貨、預購、限量、品牌專區

### 5.12 通知（Toast）
- 右上角彈出（桌機）/ 底部（手機）
- 自動消失 3 秒、可手動關閉
- 變體：成功 / 警告 / 錯誤 / 資訊

### 5.13 Empty State / Loading
- 空狀態用線稿插畫（之後再產生），文案要釣具相關（例：「目前還沒有上架，先去看看其他釣竿」）
- Loading：骨架屏（skeleton），不是 spinner

### 5.14 Footer
- 三欄：客服資訊 / 商店資訊（聯絡 / 地址 / 營業時間）/ 社群連結（FB 社團）
- 底列：版權、隱私、條款
- 配色用 `--ink-900`、文字 `--ink-300`，連結 hover 轉 `--ink-100`

---

## 6. 頁面範本（Page Templates）

### 6.1 首頁
1. **Hero**（全寬，60vh）：標語 + CTA「逛全部釣竿」
2. **11 大類入口**（icon grid，每類一個圖示 placeholder）
3. **熱銷新品**（商品卡片橫滑，6–8 件）
4. **特價精選**（商品卡片網格，最多 12 件）
5. **品牌專區**（品牌 logo 牆 placeholder：SHIMANO、DAIWA、上興、AGK、HEXING、pokee、gamakatsu …）
6. **內容區**（如新手指南、保養知識，可選）
7. **Footer**

### 6.2 分類頁
- 麵包屑 + 分類名 + 商品總筆數
- 左 sidebar（11 大類）/ 右商品網格 + 分頁
- 排序與篩選列在格線正上方
- 商品超過 80 筆使用分頁或無限滾動（建議「載入更多」按鈕）

### 6.3 商品詳情頁
- 見 §5.5
- SEO：`<title>` = 品牌 + 編號 + 商品名、`<meta description>` = 商品名 + 規格 + 價格

### 6.4 搜尋結果頁
- 與分類頁版型一致，標題改為「搜尋『關鍵字』的結果」

### 6.5 訂單查詢頁
- 簡單表單：訂單編號 + 手機末四碼 / Email → 查詢結果卡片

### 6.6 會員 / 註冊 / 登入
- 二欄式：左品牌氛圍 placeholder / 右表單
- 行動裝置：表單置中

### 6.7 FAQ
- 折疊面板（accordion），按類別分組
- 搜尋框、可直接展開全部

### 6.8 詢問 / 客服
- 表單 + 客服聯絡資訊 + LINE/Facebook QR placeholder
- 表單欄位：姓名、聯絡方式、商品編號（選填）、訊息

---

## 7. 動效與互動（Motion）

| 場景 | 動效 |
| --- | --- |
| 連結 hover | `transition: 150ms ease-out` 顏色變化 |
| 卡片 hover | `transform: scale(1.02)` + 陰影漸入 200ms |
| Modal 開啟 | 透明度 0→1、translateY 8px→0，200ms |
| 分頁/篩選變更 | 商品格線淡入 200ms，先骨架後內容 |
| Toast | 滑入 200ms、滑出 200ms |
| 全域 | 尊重 `prefers-reduced-motion`，全部動效收斂為瞬時 |

---

## 8. 無障礙（Accessibility）

- 對比度：正文 ≥ 4.5:1、大字與圖示 ≥ 3:1
- 所有圖片 `alt`，裝飾性圖 `alt=""`
- 鍵盤可達：所有可點元素皆可 Tab，focus 樣式明確（不可 `outline: none` 不補）
- 表單錯誤要文字 + 圖示，不只靠顏色
- 中文閱讀方向左到右；行寬不超過 80 中文字
- 連結加入語意 aria-label（特別是純圖示連結）

---

## 9. 圖像方向（Imagery Guidelines）

> **本階段不產任何圖**，以下作為未來生成圖時的方向：

### 9.1 商品圖
- 一致背景：純色或極簡漸層（淺灰 / 深海色）
- 角度：主圖正側、可加上 30° 透視作補充
- 比例：1:1（列表）+ 4:5（詳情主視覺）
- 細節圖：紋路、配件、刻度近拍
- 不要：擺在木桌、雜物堆、傳統閃光大背景

### 9.2 形象圖 / 情境圖
- 主題：磯岸夜釣、船釣甲板、晨曦灘釣、夜市釣蝦場
- 色調：自然光、低彩度、可帶霧氣或波光
- 構圖：人物背影或局部、強調器材與環境關係
- 不要：刻意擺拍、戴著太陽眼鏡比讚的合成照

### 9.3 圖示（Icon）
- 線稿風格、stroke 1.5px、圓角端點
- 套件：Lucide / Phosphor 風格優先
- 11 大類各一圖示（之後再產），統一視覺

### 9.4 Hero / 首頁主視覺
- 大畫面、地平線在 1/3 處、留出文字疊放區
- 海面為主、避免直射陽光在鏡頭

---

## 10. 內容與口吻（Tone of Voice）

| 場景 | 範例 |
| --- | --- |
| Hero 標語 | （未定）方向：強調「精選 / 老店 / 全品項」 |
| 商品卡片 | 簡潔：品牌 · 商品名 · 價格 |
| 特價標 | 「特價」「限量」「滿千免運」用詞收斂 |
| 空狀態 | 帶釣具情境：「這個分類目前沒有商品，去看看其他釣竿吧」 |
| 錯誤訊息 | 不嚇人：「網路訊號好像不太穩，請再試一次」 |
| 客服回應 | 直、不過度客氣 |

中英並列時：
- 中文在前、英文在後，括號或破折號分隔
- 品牌名保留原始大小寫：SHIMANO、DAIWA、HEXING、pokee

---

## 11. 暗色模式 / 亮色模式

- **預設 Light**：白底卡片、攝影為主、商品為主角（依 §14 mockup）
- 局部深色塊：Hero overlay、品牌牆、Footer
- **可切 Dark**：戶外氛圍偏好者；放在 Header 月亮 icon
- 兩種模式都要過對比度測試

---

## 12. 效能與技術約束（給未來實作參考）

- **SSG**：Astro（Content Collections 直接吃 JSON）
- **CSS**：原生 CSS Variables + 一份 `tokens.css`（不引入 Tailwind 以免設計被框架制約；之後想換可換）
- **互動**：純 vanilla JS（IIFE 模組化）或 Astro Islands（如即時庫存）
- **圖片**：使用 `<img loading="lazy" decoding="async">`、未來引入 `astro:assets` 自動產生 WebP/AVIF
- **字型**：`font-display: swap`、preconnect Google Fonts、把 Noto Sans TC 子集化或自託管
- **可長期維護**：所有顏色、字級、間距走 token，不在元件內硬編碼

---

## 13. 變更紀錄

| 版本 | 日期 | 內容 |
| --- | --- | --- |
| v0.1 | 2026-05-18 | 初稿：色彩、字體、間距、元件、頁面範本、圖像方向、Tone |
| v0.2 | 2026-05-19 | 加入 §14 參考視覺稿（首頁 mockup、設計系統總覽） |

---

## 14. 參考視覺稿（Reference Mockups）— **主規範**

> ⚠️ **本節為視覺主規範**。§1–§13 描述 token、元件結構與互動細節，
> 但**整體調性、版型節奏、佈景明暗、攝影 vs 圖示的選擇**，以本節兩張 PNG 為唯一準則。
> 當 token 細節與 mockup 整體感衝突時，**改 token 去迎合 mockup**，不要反過來。

### 14.1 首頁總覽 — `references/homepage-mockup.png`
桌機版首頁全景示意，可用來對齊 §6.1 首頁範本的節奏。

涵蓋區塊（由上至下）：
1. **Top utility bar**：左側電話 / 客服連結，右側會員 · 訂單查詢 · 語系
2. **Header**：白底、左 Logo「FISHTOOL」、中央水平 nav（首頁 / 釣具 / 釣餌 / 配件 / 品牌 / 知識 / 客服）、右側搜尋 + 會員 + 購物車徽章
3. **Hero**：深海色大圖（磯岸釣者剪影）+ 中文主標「探索無限可能 釣出你的精彩」+ 雙 CTA
4. **品牌色票 / token 條**：右側並列展示 brand swatch（提示本站偏深海主色 + 強調橘）
5. **11 大類入口**：圓形 icon + 中文標籤的網格（對應 §6.1 步驟 2、§5.7 sidebar 的同一組分類視覺）
6. **熱門產品**：4 欄商品卡片橫排，符合 §4.3 桌機 4 欄
7. **雙 promo banner**：「新品上市」「年中慶典」深色長條，左文右圖
8. **第二排商品卡 + 篩選 chip**：說明商品列表頁的格線密度
9. **品牌牆**：SHIMANO / DAIWA / 等 logo placeholder，對應 §6.1 步驟 5
10. **Footer**：深 `--ink-900` 底、三欄資訊 + 底部版權列

**對齊重點**
- 整體採 **Dark Hero + Light Section** 混搭，符合 §2.5「預設 Dark、可切 Light」之中的「Hero 暗 / 列表亮」變體
- 商品卡片**無陰影**、僅靠商品圖本身吸睛，符合 §4.4 末段
- 強調橘只出現在「特價」「限量」徽章，符合 §2.2 末段的克制原則

**注意（與本文不一致處）**
- 視覺稿上的 nav 為 7 項精簡版，本站實際為 §5.1 提到的 **11 大類**，實作時以本文為準
- 視覺稿 hero 是右上排深海色票條，僅為設計示意，**正式上線不會出現色票條**

### 14.2 設計系統總覽 — `references/design-system-overview.png`
單張海報式 style guide，把品牌主色、字體、按鈕、icon、卡片、banner、mobile mockup 全部攤平在同一張畫面，方便目視對齊。

涵蓋區塊：
- **品牌頭**：FishTool logo + 副標「品牌規範與重點設計示意」+ 簡介圖
- **色彩**：深海主色 + 中性灰 + 強調橘色塊
- **字體**：中英主字、字級階層示意
- **按鈕 / 表單**：Primary / Secondary / Pill chip / Input 樣式
- **Icon set**：線稿圖示牆（對應 §9.3 線稿風格、stroke 1.5）
- **商品卡變體**：1:1 + 條列資訊的卡片三種變體
- **Banner 範例**：深色 hero、雙欄 promo
- **Footer 範例**：深底三欄 + 底部版權列
- **Mobile mockup**：手機版商品列表 + 商品詳情雙截圖，對應 §5.5 末段「手機版圖庫橫滑、加入購物車 sticky」

**對齊重點**
- icon 風格、按鈕 radius、卡片密度 → 實作時可拿這張對照
- mobile mockup 的「sticky 加入購物車條」是 §5.5 的具象化，元件實作可參考其高度與按鈕比例

**注意（與本文不一致處）**
- 色彩格子的 HEX 取色可能與 §2 略有差異（視覺稿是 ChatGPT 產生的近似色），**以 §2 表格內的 token 值為準**
- 字體展示用了英文字種，實際中文以 §3.1 的 Noto Sans TC 為主
